## Codebase Patterns
- FileSystem: Use `expo-file-system` modern API (`Paths`, `File`, `Directory`) as legacy API is deprecated/runtime-throwing.
- Web support requires `react-dom` and `react-native-web` for browser verification.

## 2026-01-26 - US-001
- Implemented `ImageService` using `expo-file-system` v19+ API.
- Created `ImageImporter` component.
- Updated `App.tsx` to include `ImageImporter`.
- **Learnings for future iterations:**
  - `expo-file-system` has a breaking change in v19 (beta/alpha?) where legacy constants like `documentDirectory` are gone or require legacy import. Used `Paths.document` instead.
  - Verification on web requires installing web dependencies.
  - Added `CLAUDE.md` with FileSystem patterns.
---
