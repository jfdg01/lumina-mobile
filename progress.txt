## Codebase Patterns
- FileSystem: Use `expo-file-system` modern API (`Paths`, `File`, `Directory`) as legacy API is deprecated/runtime-throwing.
- Web support requires `react-dom` and `react-native-web` for browser verification.
- Animations: Reanimated requires `babel.config.js` plugin, `babel-preset-expo`, and `GestureHandlerRootView` wrapping.
- Persistence: Use `@react-native-async-storage/async-storage` for simple key-value storage (works on web and native).
- Reanimated worklets: Use `runOnJS(fn)()` to call JS functions from gesture handler callbacks.
- Wake Lock: Use `expo-keep-awake` with `activateKeepAwakeAsync`/`deactivateKeepAwake`. Always cleanup on unmount.
- Global Gestures: Wrap the parent container (not the target element) with `GestureDetector` to capture gestures anywhere on screen.
- Layout Stability: Use `absolute` positioning for workspaces that must remain static while UI overlays toggle on/off. Avoid flex-based layout shifts.
- Storage Structure: For simple lists, use a "Collection" key (JSON object map) + "Current ID" key pattern. Avoid separate keys per item unless necessary for performance.

## 2026-01-26 - US-001
- Implemented `ImageService` using `expo-file-system` v19+ API.
- Created `ImageImporter` component.
- Updated `App.tsx` to include `ImageImporter`.
- **Learnings for future iterations:**
  - `expo-file-system` has a breaking change in v19 (beta/alpha?) where legacy constants like `documentDirectory` are gone or require legacy import. Used `Paths.document` instead.
  - Verification on web requires installing web dependencies.
  - Added `CLAUDE.md` with FileSystem patterns.
---

## 2026-01-26 - US-002
- Implemented `AlignmentWorkspace` with Reanimated gestures (Pan, Pinch, Rotate).
- Integrated `AlignmentWorkspace` into `App.tsx` with conditional "Image Selected" state.
- Added `babel.config.js` for Reanimated support.
- Fixed web compatibility: `expo-file-system` doesn't work on web, bypass with `Platform.OS === 'web'` check.
- Added "Use Demo Image" button for browser testing (native file picker can't be automated).
- **Learnings for future iterations:**
  - `react-native-reanimated` requires `babel.config.js` with `react-native-reanimated/plugin`.
  - Adding `babel.config.js` requires installing `babel-preset-expo`.
  - `GestureHandlerRootView` must wrap the app root for gestures to work.
  - `expo-file-system` throws on web - use `Platform.OS` check to skip file system operations on web.
  - Native file picker dialogs can't be tested via browser automation; add demo/test buttons for verification.
---

## 2026-01-26 - US-003
- Implemented Edit/Projection mode toggle in `App.tsx`.
- Updated `AlignmentWorkspace` to disable gestures and hide "Reset" button in Projection Mode.
- Implemented UI conditional rendering to hide headers and extra buttons in Projection Mode.
- Added visual dashed border indicator for Edit Mode.
- **Learnings for future iterations:**
  - Conditional rendering in React Native for "modes" works well for projection-style apps.
  - `Gesture.Pan().enabled(false)` effectively disables interactions for projection safety.
  - Dimming the UI toggle in Projection Mode (low opacity) keeps it accessible but non-intrusive for projectors.
---

## 2026-01-26 - US-004
- Created `StorageService.ts` with AsyncStorage integration for project persistence.
- Defined `ProjectState` and `TransformState` interfaces for structured storage.
- Updated `AlignmentWorkspace` to accept initial transform values and expose `onTransformChange` callback.
- Updated `App.tsx` to load saved project on launch and auto-save transforms (debounced 500ms).
- Added loading state handling during initial project restoration.
- Files changed: `src/services/StorageService.ts` (new), `src/App.tsx`, `src/components/AlignmentWorkspace.tsx`
- **Learnings for future iterations:**
  - AsyncStorage works well on both web and native for simple key-value persistence.
  - Debouncing saves (500ms) prevents excessive storage writes during rapid gesture updates.
  - `runOnJS()` is required to call JS functions from Reanimated gesture handlers (worklet context).
  - Project state includes: `id`, `imageUri`, `transform` (x, y, scale, rotation), `lastModified` timestamp.
  - Loading state (`isLoading`) prevents UI flash when restoring saved project.
---

## 2026-01-26 - US-005
- Installed `expo-keep-awake` package.
- Added `activateKeepAwakeAsync` and `deactivateKeepAwake` imports to `App.tsx`.
- Created useEffect that manages wake lock based on `isProjectionMode` state.
- Wake lock activates when entering Projection Mode; deactivates in Edit Mode.
- Cleanup ensures wake lock is deactivated on component unmount.
- Files changed: `src/App.tsx`
- **Learnings for future iterations:**
  - `expo-keep-awake` is async on activation (`activateKeepAwakeAsync`) but sync on deactivation (`deactivateKeepAwake`).
  - Always cleanup wake lock on unmount to prevent orphaned locks.
  - Wake lock doesn't have a visible effect in web browsers (platform limitation), but works on native.
  - Dark background was already enforced from US-003 implementation (Projection Mode sets `backgroundColor: '#000'`).
---

## 2026-01-26 - US-006
- Restructured `AlignmentWorkspace` to wrap entire workspace with `GestureDetector`.
- Previously, gestures only worked when touching the image container directly.
- Now pinch, rotation, and pan gestures work anywhere on the screen.
- The `Animated.View` for workspace captures all gesture input, while transforms only apply to the image.
- Files changed: `src/components/AlignmentWorkspace.tsx`
- **Learnings for future iterations:**
  - To make gestures work globally (full screen), wrap the parent container with `GestureDetector`, not just the target element.
  - The animated transform can still be applied to a child element while capturing gestures on a parent.
  - Multi-touch gestures (pinch/rotate) require actual touch hardware to test; browser verification confirms UI stability only.
---

## 2026-01-26 - US-007
- Redesigned `App.tsx` layout to use absolute positioning for the main workspace.
- The workspace now fills the entire screen (`top: 0, bottom: 0, left: 0, right: 0`) and sits strictly behind the UI.
- Header and control buttons are now overlays (`position: 'absolute'`) so their presence doesn't affect the workspace layout flow.
- Verified mathematically that workspace center is invariant to header visibility.
- Files changed: `src/App.tsx`
- **Learnings for future iterations:**
  - When an element's position must be "pixel-perfect" invariant to other UI changes (like a toggling header), DO NOT rely on Flexbox flow.
  - Use `position: 'absolute'` for the invariant element and overlay variable elements on top.
  - While browser verification tools can be flaky, the layout logic (absolute positioning vs flex flow) provides a strong guarantee of stability.
---

## 2026-01-26 - US-008
- Implemented Multi-Project Management.
- Refactored `StorageService` to store a collection of projects (`Record<string, ProjectState>`) and a separate `CURRENT_PROJECT_ID`.
- Added `ProjectList` component with ability to Select, Create, and Delete projects.
- Updated `App.tsx` navigation flow:
  - If no project active: Show Project List.
  - If "Create New": Show Image Importer.
  - If Project Active: Show Workspace.
  - Added "← Projects" button to return to list.
- Files changed: `src/services/StorageService.ts`, `src/App.tsx`, `src/components/ProjectList.tsx` (new).
- **Learnings for future iterations:**
  - For simple local-first apps, storing the entire "db" in one AsyncStorage key is fine and simplifies "GetAll" operations.
  - State machine in `App.tsx` (CurrentProject? Creating?) effectively manages the navigation without a full router library (react-navigation currently not needed, but might be if it grows).
---

## 2026-01-26 - US-007
- Verified US-007 (Consistent Image Position).
- Fixed a 2px scale shift where removing the 1px border in Projection Mode caused the image to grow.
- Fix: `editIndicator` now uses `borderColor: 'transparent'` vs `theme.colors.primary` instead of toggling `borderWidth`.
- Browser subagent verified 0-pixel shift in image position and size between modes.
- Files changed: `src/components/AlignmentWorkspace.tsx`
- **Learnings for future iterations:**
  - Toggling `borderWidth` (e.g. 1px to 0px) changes the content box size (in standard box model) or container size, which can shift centered content.
  - To implement "Edit/Preview" modes without layout shifts, keep the layout geometry (borders, padding, margins) identical and only toggle visibility (opacity, color).
  - Browser verification is critical for pixel-perfect requirements; "looks good" isn't enough.
---

## 2026-01-26 - US-009
- Implemented Safe Launch Mode (Default to Projection).
- Updated `App.tsx` to set `isProjectionMode: true` on project load, selection, and creation.
- Updated UI toggle label to "Unlock for Editing" when in Projection Mode for better clarity.
- Verified behavior with browser automation: Selecting a project enters Projection Mode by default.
- Files changed: `src/App.tsx`, `prd.json`
- **Learnings for future iterations:**
  - Initializing state to `true` in various lifecycle methods (selection, load) ensures a "safe-by-default" experience.
  - Labels like "Unlock for Editing" are more intuitive for safety-first features than generic "Exit Projection".
---

## 2026-01-26 20:50:44 - US-012
- Migrated legacy `SafeAreaView` to `react-native-safe-area-context`.
- Wrapped the application root with `SafeAreaProvider`.
- Updated `App.tsx` to use the modern `SafeAreaView` for the header overlay.
- Verified that images do not shift when toggling between Edit and Projection modes.
- **Learnings for future iterations:**
  - `react-native-safe-area-context` is the standard for modern React Native apps to handle insets correctly.
  - When using absolute positioning for a background layer, ensure the `SafeAreaView` only wraps the UI overlays to avoid unintended padding on the workspace itself.
---

## 2026-01-26 21:05:00 - US-013
- Implemented Safe Project Navigation via long-press trigger.
- Added LongPress gesture (1s) to `AlignmentWorkspace.tsx`.
- Added `showSecondaryControls` state to `App.tsx` to manage hidden UI.
- Revealed "← Projects" and "Hide Controls" buttons only after long-press in Projection Mode.
- Verified that exiting projects works without unlocking the canvas for editing.
- **Learnings for future iterations:**
  - Real-world "Safety First" UI often requires non-obvious triggers (like long press) to avoid accidental activation.
  - `Gesture.LongPress()` integrates seamlessly with `Gesture.Simultaneous()` but requires `runOnJS` if it modifies React state.
  - UI overlays in projection apps should be "dimmed and hidden" by default but "discoverable" via specific interactions.
---

## 2026-01-26 - US-010
- Implemented Undo/Redo functionality.
- Added history stacks (undo/redo) to `App.tsx`.
- Updated `AlignmentWorkspace.tsx` to allow parent updates to shared values.
- Added Undo/Redo buttons to the Edit Mode UI.
- **Learnings for future iterations:**
  - Reanimated shared values must be updated in a `useEffect` if the source of truth moves to a parent component (controlled pattern).
  - Use `setUndoStack(prev => [...prev, currentProject.transform])` to ensure history is captured before the state update.
  - History is currently in-memory; consider persisting it if UX requirements demand recovery after restart.
---

## 2026-01-26 - US-011
- Implemented "Premium" UI Polish & Theming with Cyber Cyan palette.
- Refactored `App.tsx` to include Reanimated fade transitions for overlays.
- Standardized UI elements (buttons, project items) using theme tokens.
- Added glow effects and high-contrast surfaces for better projection visibility.
- Files changed: `src/styles/theme.ts`, `src/App.tsx`, `src/components/AlignmentWorkspace.tsx`, `src/components/ProjectList.tsx`, `src/components/ImageImporter.tsx`.
- **Learnings for future iterations:**
  - Modern "Premium" aesthetics in React Native can be achieved with consistent theme tokens and subtle shadows/glows.
  - Using `rgba` for overlays (glassmorphism) improves depth and maintains context.
  - Reanimated `useSharedValue` and `withTiming` provide a declarative way to handle UI state transitions (like showing/hiding headers).
  - Explicit `textTransform: 'uppercase'` and `letterSpacing` elevate the technical/professional feel of the UI.
---
