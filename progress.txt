## Codebase Patterns
- FileSystem: Use `expo-file-system` modern API (`Paths`, `File`, `Directory`) as legacy API is deprecated/runtime-throwing.
- Web support requires `react-dom` and `react-native-web` for browser verification.
- Animations: Reanimated requires `babel.config.js` plugin, `babel-preset-expo`, and `GestureHandlerRootView` wrapping.
- Persistence: Use `@react-native-async-storage/async-storage` for simple key-value storage (works on web and native).
- Reanimated worklets: Use `runOnJS(fn)()` to call JS functions from gesture handler callbacks.
- Wake Lock: Use `expo-keep-awake` with `activateKeepAwakeAsync`/`deactivateKeepAwake`. Always cleanup on unmount.
- Global Gestures: Wrap the parent container (not the target element) with `GestureDetector` to capture gestures anywhere on screen.
- Layout Stability: Use `absolute` positioning for workspaces that must remain static while UI overlays toggle on/off. Avoid flex-based layout shifts.
- Storage Structure: For simple lists, use a "Collection" key (JSON object map) + "Current ID" key pattern. Avoid separate keys per item unless necessary for performance.
- App Structure: `src` is the root for source. `global.css` in `src`. `@` alias maps to `./src`.
- Components: All components in `src/components`. Gluestack UI in `src/components/ui`.
- Image Handling: Use Gluestack `Image` with `source` prop. For local images, `uri` works. `Image` component needs `size` (e.g., `2xl` or `none` + `className` for custom dimensions).
## 2026-01-27 02:45 - US-005
- Refactored `AlignmentWorkspace.tsx` to use Gluestack UI components (`Box`, `HStack`, `Pressable`, `Button`, `Icon`).
- Implemented "Glassmorphism" styling for overlay controls.
- Integrated `lucide-react-native` icons.
- Delegates Undo/Redo/Exit/Projection logic from `App.tsx` down to the workspace.
- Fixed Babel conflict between `react-native-worklets` and `react-native-reanimated`.
- Verified in browser with screenshots.
- Passed typecheck.
## 2026-01-27 02:20 - US-004
- Refactored `ImageImporter.tsx`.
- Installed missing Gluestack components: `spinner`, `image`.
- Replaced Native components with `Box`, `VStack`, `Center`, `Heading`, `Text`, `Button`, `Image`, `Spinner`.
- Added `isLoading` state for import process.
- Verified appearance and transition in browser (screenshot captured).
- Typecheck passed.
**Learnings for future iterations:**
- Missing Gluestack components can be added via `npx gluestack-ui add [component-name]`.
- Always check `src/components/ui` to see what is already installed.
- Gluestack `Image` component uses `source` prop compatible with RN Image.
## 2026-01-27 01:45 - US-003
- Refactored `ProjectList.tsx` to use Gluestack UI components (`Box`, `Text`, `Heading`, `Button`, `VStack`, `HStack`).
- Replaced custom StyleSheet with NativeWind utility classes.
- Verified appearance and functionality in browser.
- Typecheck passed.
## 2026-01-27 01:25 - US-002
- Installed Gluestack components: `vstack`, `hstack`, `center`, `heading`.
- Refactored `App.tsx` entire render loop to use `Box`, `VStack`, `HStack`, `Heading`, `Text`, `Button`.
- Removed `StyleSheet` usage in `App.tsx` favor of NativeWind (`className`).
- Removed `src/styles/theme.ts` import from `App.tsx` (marked for deletion once other US are done).
- Fixed `GestureHandlerRootView` and various typing issues.
- Passed typecheck.
## 2026-01-27 01:00 - US-001
- Deprecated `src/styles/theme.ts`.
- Restored missing `AlignmentWorkspace.tsx` and `ImageImporter.tsx` which caused build failures.
- Verified `GluestackUIProvider` setup and `global.css`.
- Verified `tailwind.config.js`.
- Passed typecheck.
**Learnings for future iterations:**
- Always check `git log` if files are missing unexpectedly.
- `src/components/ui` contains the Gluestack components.

## 2026-01-27 12:25 - US-005
- Refactored `AlignmentWorkspace.tsx` to use Gluestack UI components (`Box`, `HStack`, `Pressable`, `Button`, `Icon`).
- Implemented "Glassmorphism" styling for overlay controls.
- Integrated `lucide-react-native` icons.
- Added persistent but subtle 'Unlock' button in projection mode for easier navigation on web.
- Fixed Reanimated `FadeIn`/`FadeOut` usage and imports.
- Verified in browser: controls correctly fade out/in and 'Unlock' toggle works perfectly.
- Passed typecheck.
**Learnings for future iterations:**
- Web-first users expect visible toggles; non-obvious triggers like Long Press should be secondary or paired with subtle UI cues.
- Reanimated layout animations like `FadeIn` must be explicitly imported from `react-native-reanimated` when used in `entering`/`exiting` props.
---
